<div class="general-forms">
  <div class="form-container col-md-4 col-sm-5 col-xs-7">
    <h3 class="">Invite new user</h3>
    <%= form_for @user, html: { class: "signin card-panel" } do |f| %>
      <div class="form-entries">
        <%= f.text_field :name, placeholder: 'Enter new user full name', class: 'form-control' %>
      </div>

      <div class="form-entries">
        <%= f.email_field :email, placeholder: 'Enter new user email', class: 'form-control' %>
      </div>

      <div>
        <%= f.submit 'Invite', class: 'general-button login-btn' %>
      </div>
    <% end %>
  </div>
</div>
